import random
import uuid
from datetime import datetime, timedelta
import pandas as pd
from collections import defaultdict
# generate n participants 

# generate cycles (participants, min and max amount)

# generate obligations (cycles, noise factor, start time)