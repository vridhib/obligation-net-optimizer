[pytest]
DJANGO_SETTINGS_MODULE = config.settings
addopts = --tb=short --strict-markers
markers =
    django_db: marks tests that need database access
    slow: marks slow tests (for optional filtering)